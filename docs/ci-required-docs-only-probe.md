# CI Required docs-only probe

Temporary verification that docs-only pull requests still start CI Unified, skip the heavy CI jobs, and produce `CI Required` as a successful aggregate check.

This file is only for validating the repository protection workflow and is not intended to be merged.
