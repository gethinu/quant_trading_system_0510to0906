システム6ーショート・ミーン・リバージョン・ハイ・シックスデイサージ

トレード対象
NYSE、ナスダック、アメリカン証券取引所に上場しているすべての銘柄。

フィルター
	•	 最低株価が5ドル以上。
	•	 過去 50 日の平均売買代金が 1000万ドルを上回る。
	•	 50日ヒストリカルボラティリティ（HV50）が 10〜40% の範囲内にある。
		・ 実装参照: core/system6.py::HV50_BOUNDS_PERCENT (= (10.0, 40.0))。
		・ 由来: System4 の HV bounds に整合させた operational refinement。異常高ボラ (news-driven / broken stock) 除外の safety。

セットアップ
	•	 直近6日間で株価が20%以上上昇。
	•	 直近2日は前日の終値を上回って引ける。

【重要】閾値に関する注意事項:
	•	 return_6d > 0.20 (6日間で20%上昇)は非常に厳しい条件です。
	•	 通常の市場環境では、この条件を満たす銘柄は少数または0になることがあります。
	•	 これは仕様通りの正常な動作であり、エラーではありません。
	•	 候補数が0の場合でも、システムは正常に動作しています。
	•	 強いモメンタムが発生している相場局面でのみ候補が出現します。

実装詳細:
	•	 コード上の条件: return_6d > 0.20 AND uptwodays == True
	•	 predicate関数: common/system_setup_predicates.py::system6_setup_predicate()
	•	 候補生成: core/system6.py::generate_candidates_system6()

ランキング
直近6日間の株価の上昇が大きい順に銘柄を選ぶ。

仕掛け
前日の終値を5%上回る位置に指値を置いて売る。

損切り注文
売値の上に、過去10日の 3ATR の位置に損切り注文を置く。

再仕掛け
損切りに引っかかったら再び仕掛ける。

利益の保護
使わない。

利食い
	•	 5%の利益が出たら、翌日の大引けで成り行きで手仕舞う。
	•	 時間ベースの利食いを使い、仕掛けてから3日後には大引けで成り行きで手仕舞う。

ポジションサイジング
リスクは資金全体の2%、サイズは最大で資金全体の 10%。

---
【変更履歴】
2026-07-02 impl-alignment update:
	•	 フィルターに HV50 10〜40% の範囲条件を追記。
	•	 実装 (core/system6.py::HV50_BOUNDS_PERCENT = (10.0, 40.0)) との乖離を解消。
	•	 由来: System4 の HV bounds から borrowed した operational refinement。異常高ボラ (news-driven / broken stock) 除外の safety として維持。
	•	 backtest 影響: 無し（docs 側のみの更新、code 挙動変更なし）。

2026-07-03 D3 alignment audit:
	•	 System6 のフィルター (Low>=5, DV50>10M, HV50 in [10, 40]) は既に impl と docs で完全整合済み。
	     この dispatch では追加変更なし (verify only)。
	•	 参考: docs/D3_CASE_A_IMPL_20260703.md の全 system alignment matrix。
