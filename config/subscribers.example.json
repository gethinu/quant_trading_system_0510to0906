{
  "_comment": "Phase 2 配信の下地。config/subscribers.json にコピーして使う。secret は env 変数名で参照し、生値は置かない。Phase 1 では PublisherRegistry (ntfy primary + email backup) が全 system を自分へ配信する。Phase 2 で tier / systems フィルタと per-subscriber fan-out を実装する。",
  "version": "1.0",
  "subscribers": [
    {
      "id": "owner",
      "name": "self (Phase 1)",
      "tier": "internal",
      "systems": ["*"],
      "channels": [
        { "type": "ntfy", "topic_env": "NTFY_TOPIC" },
        { "type": "email", "to_env": "SENDGRID_TO_EMAIL" }
      ]
    },
    {
      "id": "beta_user_001",
      "name": "beta subscriber example (Phase 2)",
      "tier": "basic",
      "systems": ["sys1", "sys4", "sys7"],
      "channels": [
        { "type": "ntfy", "topic_env": "BETA001_NTFY_TOPIC" },
        { "type": "email", "to": "beta001@example.com" }
      ]
    }
  ]
}
